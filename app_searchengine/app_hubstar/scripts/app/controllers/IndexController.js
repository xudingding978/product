define(['models/MegaModel',
    'ember'],
        function(
                MegaModel,
                Ember
                ) {
            var IndexController = Ember.ArrayController.extend({
 
            });
            return IndexController;
        });
